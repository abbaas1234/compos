import { RepeatTemplateDirective } from './repeat-template.directive';

describe('RepeatTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
